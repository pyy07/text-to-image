# 说明：
# - 由于某些环境会屏蔽 `.env.example`（匹配 *.env*），这里用 `env.example` 作为模板。
# - 你本地使用时：`cp env.example .env` 然后按需修改。

# ----------------------------
# 基础
# ----------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:3000"

# 本地测试：是否允许匿名生成（开发环境默认允许；显式设置 false 可关闭）
ALLOW_ANONYMOUS="true"

# ----------------------------
# 文生图（OpenAI 兼容接口 / modelscope z-image）
# ----------------------------
AI_PROVIDERS="openai"
AI_PROVIDER="openai"

# modelscope（或其他 OpenAI 兼容服务）的 key
OPENAI_API_KEY="your-openai-compatible-key"

# OpenAI 兼容接口地址（以 /v1 结尾）
OPENAI_BASE_URL="https://your-openai-compatible-endpoint/v1"

# 允许的模型列表（用于后端校验）
OPENAI_MODELS="z-image"

# 默认模型（不填则取 OPENAI_MODELS 的第一个）
OPENAI_MODEL="z-image"

# 默认图片尺寸（images.generate 的 size）
OPENAI_IMAGE_SIZE="1024x1024"

# ----------------------------
# 文生漫（独立开关与 Gemini，可与文生图分离）
# ----------------------------
# 是否开启文生漫：true 时可用，false 时入口仍展示、提交时提示联系管理员
COMIC_ENABLED="false"

# 文生漫专用 Gemini API 地址（@google/genai 的 httpOptions.baseUrl）
# - Google 官方：https://generativelanguage.googleapis.com/v1beta
# - UniAPI 代理：https://api.uniapi.io/gemini
COMIC_GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta"

# 文生漫专用 API Key（与 OPENAI_API_KEY / GOOGLE_AI_API_KEY 分离）
COMIC_GEMINI_API_KEY=""

# 文生漫模型：须支持「出图」（responseModalities: IMAGE），否则会返回 NO_IMAGE
# 请按代理/Google 文档选用可出图模型（如 gemini-2.0-flash-preview-image 等）
COMIC_GEMINI_MODEL="gemini-2.0-flash-exp"

# 案例列表（图片+漫画）是否允许删除，设为 true 时在案例页显示删除按钮
ALLOW_DELETE="false"

# ----------------------------
# 对象存储：Vercel Blob（只存 URL，不双写）
# ----------------------------
# Vercel Project -> Storage -> Blob -> Read/Write Token
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"

# （可选）上传路径前缀，会上传到 `${VERCEL_BLOB_PREFIX}/...`
VERCEL_BLOB_PREFIX="generated"

# ----------------------------
# 微信登录（可选）
# ----------------------------
WECHAT_APP_ID="your_wechat_app_id"
WECHAT_APP_SECRET="your_wechat_app_secret"

# ----------------------------
# 代理（可选）
# ----------------------------
HTTPS_PROXY=""
HTTP_PROXY=""


